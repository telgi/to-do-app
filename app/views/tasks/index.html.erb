<h1>To-Do List</h1>
<p>Tasks go here:</p>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <strong><%= link_to task.title, task_path(task) %></strong>
    </li>
  <% end %>
</ul>

<%= link_to 'New Task', new_task_path %>
